{
  "name": "hexo-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "1.install-hexo-cli": "npm install -g hexo-cli",
    "2.hexo-init": "npm install hexo init",
    "version": "hexo version",
    "clean": "hexo clean",
    "generate":"hexo generate",
    "preview-server":"hexo generate && hexo server --ip 127.0.0.1 --port 4001 --static --log",
    "install-sftp": "npm install hexo-deployer-sftp --save",
    "deploy": "hexo deploy"
  },
  "hexo": {
    "version": "3.8.0"
  },
  "dependencies": {
    "hexo": "^3.8.0",
    "hexo-deployer-sftp": "^0.1.0",
    "hexo-generator-archive": "^0.1.5",
    "hexo-generator-category": "^0.1.3",
    "hexo-generator-index": "^0.2.1",
    "hexo-generator-tag": "^0.2.0",
    "hexo-renderer-ejs": "^0.3.1",
    "hexo-renderer-marked": "^1.0.1",
    "hexo-renderer-stylus": "^0.3.3",
    "hexo-server": "^0.3.3"
  }
}